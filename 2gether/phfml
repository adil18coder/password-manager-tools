import hashlib

def hash_master_password(password):
    return hashlib.sha256(password.encode()).hexdigest()

# Example usage
master_pass = "MySecureMaster123!"
hashed = hash_master_password(master_pass)
print("Hashed master password:", hashed)

